(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("@redux-saga/delay-p"), require("redux-saga"), require("redux-saga/effects"));
	else if(typeof define === 'function' && define.amd)
		define(["@redux-saga/delay-p", "redux-saga", "redux-saga/effects"], factory);
	else if(typeof exports === 'object')
		exports["ReduxSagaRequests"] = factory(require("@redux-saga/delay-p"), require("redux-saga"), require("redux-saga/effects"));
	else
		root["ReduxSagaRequests"] = factory(root[undefined], root["ReduxSaga"], root["ReduxSaga.effects"]);
})(window, function(__WEBPACK_EXTERNAL_MODULE__redux_saga_delay_p__, __WEBPACK_EXTERNAL_MODULE_redux_saga__, __WEBPACK_EXTERNAL_MODULE_redux_saga_effects__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./src/index.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "../../node_modules/@babel/runtime/helpers/extends.js":
/*!************************************************************************************************!*\
  !*** /home/klis87/projects/redux-saga-requests/node_modules/@babel/runtime/helpers/extends.js ***!
  \************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _extends() {
  module.exports = _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

module.exports = _extends;

/***/ }),

/***/ "../../node_modules/@babel/runtime/helpers/objectWithoutPropertiesLoose.js":
/*!*********************************************************************************************************************!*\
  !*** /home/klis87/projects/redux-saga-requests/node_modules/@babel/runtime/helpers/objectWithoutPropertiesLoose.js ***!
  \*********************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

module.exports = _objectWithoutPropertiesLoose;

/***/ }),

/***/ "../../node_modules/@babel/runtime/regenerator/index.js":
/*!**************************************************************************************************!*\
  !*** /home/klis87/projects/redux-saga-requests/node_modules/@babel/runtime/regenerator/index.js ***!
  \**************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(/*! regenerator-runtime */ "../../node_modules/regenerator-runtime/runtime-module.js");


/***/ }),

/***/ "../../node_modules/regenerator-runtime/runtime-module.js":
/*!****************************************************************************************************!*\
  !*** /home/klis87/projects/redux-saga-requests/node_modules/regenerator-runtime/runtime-module.js ***!
  \****************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

// This method of obtaining a reference to the global object needs to be
// kept identical to the way it is obtained in runtime.js
var g = (function() {
  return this || (typeof self === "object" && self);
})() || Function("return this")();

// Use `getOwnPropertyNames` because not all browsers support calling
// `hasOwnProperty` on the global `self` object in a worker. See #183.
var hadRuntime = g.regeneratorRuntime &&
  Object.getOwnPropertyNames(g).indexOf("regeneratorRuntime") >= 0;

// Save the old regeneratorRuntime in case it needs to be restored later.
var oldRuntime = hadRuntime && g.regeneratorRuntime;

// Force reevalutation of runtime.js.
g.regeneratorRuntime = undefined;

module.exports = __webpack_require__(/*! ./runtime */ "../../node_modules/regenerator-runtime/runtime.js");

if (hadRuntime) {
  // Restore the original runtime.
  g.regeneratorRuntime = oldRuntime;
} else {
  // Remove the global property added by runtime.js.
  try {
    delete g.regeneratorRuntime;
  } catch(e) {
    g.regeneratorRuntime = undefined;
  }
}


/***/ }),

/***/ "../../node_modules/regenerator-runtime/runtime.js":
/*!*********************************************************************************************!*\
  !*** /home/klis87/projects/redux-saga-requests/node_modules/regenerator-runtime/runtime.js ***!
  \*********************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

!(function(global) {
  "use strict";

  var Op = Object.prototype;
  var hasOwn = Op.hasOwnProperty;
  var undefined; // More compressible than void 0.
  var $Symbol = typeof Symbol === "function" ? Symbol : {};
  var iteratorSymbol = $Symbol.iterator || "@@iterator";
  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

  var inModule = typeof module === "object";
  var runtime = global.regeneratorRuntime;
  if (runtime) {
    if (inModule) {
      // If regeneratorRuntime is defined globally and we're in a module,
      // make the exports object identical to regeneratorRuntime.
      module.exports = runtime;
    }
    // Don't bother evaluating the rest of this file if the runtime was
    // already defined globally.
    return;
  }

  // Define the runtime globally (as expected by generated code) as either
  // module.exports (if we're in a module) or a new, empty object.
  runtime = global.regeneratorRuntime = inModule ? module.exports : {};

  function wrap(innerFn, outerFn, self, tryLocsList) {
    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
    var generator = Object.create(protoGenerator.prototype);
    var context = new Context(tryLocsList || []);

    // The ._invoke method unifies the implementations of the .next,
    // .throw, and .return methods.
    generator._invoke = makeInvokeMethod(innerFn, self, context);

    return generator;
  }
  runtime.wrap = wrap;

  // Try/catch helper to minimize deoptimizations. Returns a completion
  // record like context.tryEntries[i].completion. This interface could
  // have been (and was previously) designed to take a closure to be
  // invoked without arguments, but in all the cases we care about we
  // already have an existing method we want to call, so there's no need
  // to create a new function object. We can even get away with assuming
  // the method takes exactly one argument, since that happens to be true
  // in every case, so we don't have to touch the arguments object. The
  // only additional allocation required is the completion record, which
  // has a stable shape and so hopefully should be cheap to allocate.
  function tryCatch(fn, obj, arg) {
    try {
      return { type: "normal", arg: fn.call(obj, arg) };
    } catch (err) {
      return { type: "throw", arg: err };
    }
  }

  var GenStateSuspendedStart = "suspendedStart";
  var GenStateSuspendedYield = "suspendedYield";
  var GenStateExecuting = "executing";
  var GenStateCompleted = "completed";

  // Returning this object from the innerFn has the same effect as
  // breaking out of the dispatch switch statement.
  var ContinueSentinel = {};

  // Dummy constructor functions that we use as the .constructor and
  // .constructor.prototype properties for functions that return Generator
  // objects. For full spec compliance, you may wish to configure your
  // minifier not to mangle the names of these two functions.
  function Generator() {}
  function GeneratorFunction() {}
  function GeneratorFunctionPrototype() {}

  // This is a polyfill for %IteratorPrototype% for environments that
  // don't natively support it.
  var IteratorPrototype = {};
  IteratorPrototype[iteratorSymbol] = function () {
    return this;
  };

  var getProto = Object.getPrototypeOf;
  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
  if (NativeIteratorPrototype &&
      NativeIteratorPrototype !== Op &&
      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
    // This environment has a native %IteratorPrototype%; use it instead
    // of the polyfill.
    IteratorPrototype = NativeIteratorPrototype;
  }

  var Gp = GeneratorFunctionPrototype.prototype =
    Generator.prototype = Object.create(IteratorPrototype);
  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;
  GeneratorFunctionPrototype.constructor = GeneratorFunction;
  GeneratorFunctionPrototype[toStringTagSymbol] =
    GeneratorFunction.displayName = "GeneratorFunction";

  // Helper for defining the .next, .throw, and .return methods of the
  // Iterator interface in terms of a single ._invoke method.
  function defineIteratorMethods(prototype) {
    ["next", "throw", "return"].forEach(function(method) {
      prototype[method] = function(arg) {
        return this._invoke(method, arg);
      };
    });
  }

  runtime.isGeneratorFunction = function(genFun) {
    var ctor = typeof genFun === "function" && genFun.constructor;
    return ctor
      ? ctor === GeneratorFunction ||
        // For the native GeneratorFunction constructor, the best we can
        // do is to check its .name property.
        (ctor.displayName || ctor.name) === "GeneratorFunction"
      : false;
  };

  runtime.mark = function(genFun) {
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
    } else {
      genFun.__proto__ = GeneratorFunctionPrototype;
      if (!(toStringTagSymbol in genFun)) {
        genFun[toStringTagSymbol] = "GeneratorFunction";
      }
    }
    genFun.prototype = Object.create(Gp);
    return genFun;
  };

  // Within the body of any async function, `await x` is transformed to
  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
  // `hasOwn.call(value, "__await")` to determine if the yielded value is
  // meant to be awaited.
  runtime.awrap = function(arg) {
    return { __await: arg };
  };

  function AsyncIterator(generator) {
    function invoke(method, arg, resolve, reject) {
      var record = tryCatch(generator[method], generator, arg);
      if (record.type === "throw") {
        reject(record.arg);
      } else {
        var result = record.arg;
        var value = result.value;
        if (value &&
            typeof value === "object" &&
            hasOwn.call(value, "__await")) {
          return Promise.resolve(value.__await).then(function(value) {
            invoke("next", value, resolve, reject);
          }, function(err) {
            invoke("throw", err, resolve, reject);
          });
        }

        return Promise.resolve(value).then(function(unwrapped) {
          // When a yielded Promise is resolved, its final value becomes
          // the .value of the Promise<{value,done}> result for the
          // current iteration.
          result.value = unwrapped;
          resolve(result);
        }, function(error) {
          // If a rejected Promise was yielded, throw the rejection back
          // into the async generator function so it can be handled there.
          return invoke("throw", error, resolve, reject);
        });
      }
    }

    var previousPromise;

    function enqueue(method, arg) {
      function callInvokeWithMethodAndArg() {
        return new Promise(function(resolve, reject) {
          invoke(method, arg, resolve, reject);
        });
      }

      return previousPromise =
        // If enqueue has been called before, then we want to wait until
        // all previous Promises have been resolved before calling invoke,
        // so that results are always delivered in the correct order. If
        // enqueue has not been called before, then it is important to
        // call invoke immediately, without waiting on a callback to fire,
        // so that the async generator function has the opportunity to do
        // any necessary setup in a predictable way. This predictability
        // is why the Promise constructor synchronously invokes its
        // executor callback, and why async functions synchronously
        // execute code before the first await. Since we implement simple
        // async functions in terms of async generators, it is especially
        // important to get this right, even though it requires care.
        previousPromise ? previousPromise.then(
          callInvokeWithMethodAndArg,
          // Avoid propagating failures to Promises returned by later
          // invocations of the iterator.
          callInvokeWithMethodAndArg
        ) : callInvokeWithMethodAndArg();
    }

    // Define the unified helper method that is used to implement .next,
    // .throw, and .return (see defineIteratorMethods).
    this._invoke = enqueue;
  }

  defineIteratorMethods(AsyncIterator.prototype);
  AsyncIterator.prototype[asyncIteratorSymbol] = function () {
    return this;
  };
  runtime.AsyncIterator = AsyncIterator;

  // Note that simple async functions are implemented on top of
  // AsyncIterator objects; they just return a Promise for the value of
  // the final result produced by the iterator.
  runtime.async = function(innerFn, outerFn, self, tryLocsList) {
    var iter = new AsyncIterator(
      wrap(innerFn, outerFn, self, tryLocsList)
    );

    return runtime.isGeneratorFunction(outerFn)
      ? iter // If outerFn is a generator, return the full iterator.
      : iter.next().then(function(result) {
          return result.done ? result.value : iter.next();
        });
  };

  function makeInvokeMethod(innerFn, self, context) {
    var state = GenStateSuspendedStart;

    return function invoke(method, arg) {
      if (state === GenStateExecuting) {
        throw new Error("Generator is already running");
      }

      if (state === GenStateCompleted) {
        if (method === "throw") {
          throw arg;
        }

        // Be forgiving, per 25.3.3.3.3 of the spec:
        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
        return doneResult();
      }

      context.method = method;
      context.arg = arg;

      while (true) {
        var delegate = context.delegate;
        if (delegate) {
          var delegateResult = maybeInvokeDelegate(delegate, context);
          if (delegateResult) {
            if (delegateResult === ContinueSentinel) continue;
            return delegateResult;
          }
        }

        if (context.method === "next") {
          // Setting context._sent for legacy support of Babel's
          // function.sent implementation.
          context.sent = context._sent = context.arg;

        } else if (context.method === "throw") {
          if (state === GenStateSuspendedStart) {
            state = GenStateCompleted;
            throw context.arg;
          }

          context.dispatchException(context.arg);

        } else if (context.method === "return") {
          context.abrupt("return", context.arg);
        }

        state = GenStateExecuting;

        var record = tryCatch(innerFn, self, context);
        if (record.type === "normal") {
          // If an exception is thrown from innerFn, we leave state ===
          // GenStateExecuting and loop back for another invocation.
          state = context.done
            ? GenStateCompleted
            : GenStateSuspendedYield;

          if (record.arg === ContinueSentinel) {
            continue;
          }

          return {
            value: record.arg,
            done: context.done
          };

        } else if (record.type === "throw") {
          state = GenStateCompleted;
          // Dispatch the exception by looping back around to the
          // context.dispatchException(context.arg) call above.
          context.method = "throw";
          context.arg = record.arg;
        }
      }
    };
  }

  // Call delegate.iterator[context.method](context.arg) and handle the
  // result, either by returning a { value, done } result from the
  // delegate iterator, or by modifying context.method and context.arg,
  // setting context.delegate to null, and returning the ContinueSentinel.
  function maybeInvokeDelegate(delegate, context) {
    var method = delegate.iterator[context.method];
    if (method === undefined) {
      // A .throw or .return when the delegate iterator has no .throw
      // method always terminates the yield* loop.
      context.delegate = null;

      if (context.method === "throw") {
        if (delegate.iterator.return) {
          // If the delegate iterator has a return method, give it a
          // chance to clean up.
          context.method = "return";
          context.arg = undefined;
          maybeInvokeDelegate(delegate, context);

          if (context.method === "throw") {
            // If maybeInvokeDelegate(context) changed context.method from
            // "return" to "throw", let that override the TypeError below.
            return ContinueSentinel;
          }
        }

        context.method = "throw";
        context.arg = new TypeError(
          "The iterator does not provide a 'throw' method");
      }

      return ContinueSentinel;
    }

    var record = tryCatch(method, delegate.iterator, context.arg);

    if (record.type === "throw") {
      context.method = "throw";
      context.arg = record.arg;
      context.delegate = null;
      return ContinueSentinel;
    }

    var info = record.arg;

    if (! info) {
      context.method = "throw";
      context.arg = new TypeError("iterator result is not an object");
      context.delegate = null;
      return ContinueSentinel;
    }

    if (info.done) {
      // Assign the result of the finished delegate to the temporary
      // variable specified by delegate.resultName (see delegateYield).
      context[delegate.resultName] = info.value;

      // Resume execution at the desired location (see delegateYield).
      context.next = delegate.nextLoc;

      // If context.method was "throw" but the delegate handled the
      // exception, let the outer generator proceed normally. If
      // context.method was "next", forget context.arg since it has been
      // "consumed" by the delegate iterator. If context.method was
      // "return", allow the original .return call to continue in the
      // outer generator.
      if (context.method !== "return") {
        context.method = "next";
        context.arg = undefined;
      }

    } else {
      // Re-yield the result returned by the delegate method.
      return info;
    }

    // The delegate iterator is finished, so forget it and continue with
    // the outer generator.
    context.delegate = null;
    return ContinueSentinel;
  }

  // Define Generator.prototype.{next,throw,return} in terms of the
  // unified ._invoke helper method.
  defineIteratorMethods(Gp);

  Gp[toStringTagSymbol] = "Generator";

  // A Generator should always return itself as the iterator object when the
  // @@iterator function is called on it. Some browsers' implementations of the
  // iterator prototype chain incorrectly implement this, causing the Generator
  // object to not be returned from this call. This ensures that doesn't happen.
  // See https://github.com/facebook/regenerator/issues/274 for more details.
  Gp[iteratorSymbol] = function() {
    return this;
  };

  Gp.toString = function() {
    return "[object Generator]";
  };

  function pushTryEntry(locs) {
    var entry = { tryLoc: locs[0] };

    if (1 in locs) {
      entry.catchLoc = locs[1];
    }

    if (2 in locs) {
      entry.finallyLoc = locs[2];
      entry.afterLoc = locs[3];
    }

    this.tryEntries.push(entry);
  }

  function resetTryEntry(entry) {
    var record = entry.completion || {};
    record.type = "normal";
    delete record.arg;
    entry.completion = record;
  }

  function Context(tryLocsList) {
    // The root entry object (effectively a try statement without a catch
    // or a finally block) gives us a place to store values thrown from
    // locations where there is no enclosing try statement.
    this.tryEntries = [{ tryLoc: "root" }];
    tryLocsList.forEach(pushTryEntry, this);
    this.reset(true);
  }

  runtime.keys = function(object) {
    var keys = [];
    for (var key in object) {
      keys.push(key);
    }
    keys.reverse();

    // Rather than returning an object with a next method, we keep
    // things simple and return the next function itself.
    return function next() {
      while (keys.length) {
        var key = keys.pop();
        if (key in object) {
          next.value = key;
          next.done = false;
          return next;
        }
      }

      // To avoid creating an additional object, we just hang the .value
      // and .done properties off the next function object itself. This
      // also ensures that the minifier will not anonymize the function.
      next.done = true;
      return next;
    };
  };

  function values(iterable) {
    if (iterable) {
      var iteratorMethod = iterable[iteratorSymbol];
      if (iteratorMethod) {
        return iteratorMethod.call(iterable);
      }

      if (typeof iterable.next === "function") {
        return iterable;
      }

      if (!isNaN(iterable.length)) {
        var i = -1, next = function next() {
          while (++i < iterable.length) {
            if (hasOwn.call(iterable, i)) {
              next.value = iterable[i];
              next.done = false;
              return next;
            }
          }

          next.value = undefined;
          next.done = true;

          return next;
        };

        return next.next = next;
      }
    }

    // Return an iterator with no values.
    return { next: doneResult };
  }
  runtime.values = values;

  function doneResult() {
    return { value: undefined, done: true };
  }

  Context.prototype = {
    constructor: Context,

    reset: function(skipTempReset) {
      this.prev = 0;
      this.next = 0;
      // Resetting context._sent for legacy support of Babel's
      // function.sent implementation.
      this.sent = this._sent = undefined;
      this.done = false;
      this.delegate = null;

      this.method = "next";
      this.arg = undefined;

      this.tryEntries.forEach(resetTryEntry);

      if (!skipTempReset) {
        for (var name in this) {
          // Not sure about the optimal order of these conditions:
          if (name.charAt(0) === "t" &&
              hasOwn.call(this, name) &&
              !isNaN(+name.slice(1))) {
            this[name] = undefined;
          }
        }
      }
    },

    stop: function() {
      this.done = true;

      var rootEntry = this.tryEntries[0];
      var rootRecord = rootEntry.completion;
      if (rootRecord.type === "throw") {
        throw rootRecord.arg;
      }

      return this.rval;
    },

    dispatchException: function(exception) {
      if (this.done) {
        throw exception;
      }

      var context = this;
      function handle(loc, caught) {
        record.type = "throw";
        record.arg = exception;
        context.next = loc;

        if (caught) {
          // If the dispatched exception was caught by a catch block,
          // then let that catch block handle the exception normally.
          context.method = "next";
          context.arg = undefined;
        }

        return !! caught;
      }

      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        var record = entry.completion;

        if (entry.tryLoc === "root") {
          // Exception thrown outside of any try block that could handle
          // it, so set the completion value of the entire function to
          // throw the exception.
          return handle("end");
        }

        if (entry.tryLoc <= this.prev) {
          var hasCatch = hasOwn.call(entry, "catchLoc");
          var hasFinally = hasOwn.call(entry, "finallyLoc");

          if (hasCatch && hasFinally) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            } else if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else if (hasCatch) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            }

          } else if (hasFinally) {
            if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else {
            throw new Error("try statement without catch or finally");
          }
        }
      }
    },

    abrupt: function(type, arg) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc <= this.prev &&
            hasOwn.call(entry, "finallyLoc") &&
            this.prev < entry.finallyLoc) {
          var finallyEntry = entry;
          break;
        }
      }

      if (finallyEntry &&
          (type === "break" ||
           type === "continue") &&
          finallyEntry.tryLoc <= arg &&
          arg <= finallyEntry.finallyLoc) {
        // Ignore the finally entry if control is not jumping to a
        // location outside the try/catch block.
        finallyEntry = null;
      }

      var record = finallyEntry ? finallyEntry.completion : {};
      record.type = type;
      record.arg = arg;

      if (finallyEntry) {
        this.method = "next";
        this.next = finallyEntry.finallyLoc;
        return ContinueSentinel;
      }

      return this.complete(record);
    },

    complete: function(record, afterLoc) {
      if (record.type === "throw") {
        throw record.arg;
      }

      if (record.type === "break" ||
          record.type === "continue") {
        this.next = record.arg;
      } else if (record.type === "return") {
        this.rval = this.arg = record.arg;
        this.method = "return";
        this.next = "end";
      } else if (record.type === "normal" && afterLoc) {
        this.next = afterLoc;
      }

      return ContinueSentinel;
    },

    finish: function(finallyLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.finallyLoc === finallyLoc) {
          this.complete(entry.completion, entry.afterLoc);
          resetTryEntry(entry);
          return ContinueSentinel;
        }
      }
    },

    "catch": function(tryLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc === tryLoc) {
          var record = entry.completion;
          if (record.type === "throw") {
            var thrown = record.arg;
            resetTryEntry(entry);
          }
          return thrown;
        }
      }

      // The context.catch method must only be called with a location
      // argument that corresponds to a known catch block.
      throw new Error("illegal catch attempt");
    },

    delegateYield: function(iterable, resultName, nextLoc) {
      this.delegate = {
        iterator: values(iterable),
        resultName: resultName,
        nextLoc: nextLoc
      };

      if (this.method === "next") {
        // Deliberately forget the last sent value so that we don't
        // accidentally pass it on to the delegate.
        this.arg = undefined;
      }

      return ContinueSentinel;
    }
  };
})(
  // In sloppy mode, unbound `this` refers to the global object, fallback to
  // Function constructor if we're in global strict mode. That is sadly a form
  // of indirect eval which violates Content Security Policy.
  (function() {
    return this || (typeof self === "object" && self);
  })() || Function("return this")()
);


/***/ }),

/***/ "./src/actions.js":
/*!************************!*\
  !*** ./src/actions.js ***!
  \************************/
/*! exports provided: success, error, abort, createSuccessAction, createErrorAction, createAbortAction, getActionPayload, isRequestAction, isResponseAction, getRequestActionFromResponse, isSuccessAction, isErrorAction, isAbortAction, getRequestCache, clearRequestsCache */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "success", function() { return success; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "error", function() { return error; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "abort", function() { return abort; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createSuccessAction", function() { return createSuccessAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createErrorAction", function() { return createErrorAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createAbortAction", function() { return createAbortAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getActionPayload", function() { return getActionPayload; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isRequestAction", function() { return isRequestAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isResponseAction", function() { return isResponseAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRequestActionFromResponse", function() { return getRequestActionFromResponse; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isSuccessAction", function() { return isSuccessAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isErrorAction", function() { return isErrorAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isAbortAction", function() { return isAbortAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRequestCache", function() { return getRequestCache; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "clearRequestsCache", function() { return clearRequestsCache; });
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/extends */ "../../node_modules/@babel/runtime/helpers/extends.js");
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _constants__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./constants */ "./src/constants.js");



var getActionWithSuffix = function getActionWithSuffix(suffix) {
  return function (actionType) {
    return actionType + suffix;
  };
};

var success = getActionWithSuffix(_constants__WEBPACK_IMPORTED_MODULE_1__["SUCCESS_SUFFIX"]);
var error = getActionWithSuffix(_constants__WEBPACK_IMPORTED_MODULE_1__["ERROR_SUFFIX"]);
var abort = getActionWithSuffix(_constants__WEBPACK_IMPORTED_MODULE_1__["ABORT_SUFFIX"]);

var isFSA = function isFSA(action) {
  return !!action.payload;
};

var createSuccessAction = function createSuccessAction(action, data, response) {
  return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({
    type: success(action.type)
  }, isFSA(action) ? {
    payload: {
      data: data,
      response: response
    }
  } : {
    data: data,
    response: response
  }, {
    meta: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({}, action.meta, {
      requestAction: action
    })
  });
};
var createErrorAction = function createErrorAction(action, errorData) {
  return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({
    type: error(action.type)
  }, isFSA(action) ? {
    payload: errorData,
    error: true
  } : {
    error: errorData
  }, {
    meta: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({}, action.meta, {
      requestAction: action
    })
  });
};
var createAbortAction = function createAbortAction(action) {
  return {
    type: abort(action.type),
    meta: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({}, action.meta, {
      requestAction: action
    })
  };
};
var getActionPayload = function getActionPayload(action) {
  return action.payload === undefined ? action : action.payload;
};
var isRequestAction = function isRequestAction(action) {
  var actionPayload = getActionPayload(action);
  return !!actionPayload && !!actionPayload.request && !!(Array.isArray(actionPayload.request) || actionPayload.request.url || actionPayload.request.query) && !actionPayload.response && !(actionPayload instanceof Error);
};
var isResponseAction = function isResponseAction(action) {
  return !!(action.meta && action.meta.requestAction);
};
var getRequestActionFromResponse = function getRequestActionFromResponse(action) {
  return action.meta.requestAction;
};
var isSuccessAction = function isSuccessAction(action) {
  return isResponseAction(action) && action.type.endsWith(_constants__WEBPACK_IMPORTED_MODULE_1__["SUCCESS_SUFFIX"]);
};
var isErrorAction = function isErrorAction(action) {
  return isResponseAction(action) && action.type.endsWith(_constants__WEBPACK_IMPORTED_MODULE_1__["ERROR_SUFFIX"]);
};
var isAbortAction = function isAbortAction(action) {
  return isResponseAction(action) && action.type.endsWith(_constants__WEBPACK_IMPORTED_MODULE_1__["ABORT_SUFFIX"]);
};
var getRequestCache = function getRequestCache() {
  return {
    type: _constants__WEBPACK_IMPORTED_MODULE_1__["GET_REQUEST_CACHE"]
  };
};
var clearRequestsCache = function clearRequestsCache() {
  for (var _len = arguments.length, actionTypes = new Array(_len), _key = 0; _key < _len; _key++) {
    actionTypes[_key] = arguments[_key];
  }

  return {
    type: _constants__WEBPACK_IMPORTED_MODULE_1__["CLEAR_REQUESTS_CACHE"],
    actionTypes: actionTypes
  };
};

/***/ }),

/***/ "./src/constants.js":
/*!**************************!*\
  !*** ./src/constants.js ***!
  \**************************/
/*! exports provided: SUCCESS_SUFFIX, ERROR_SUFFIX, ABORT_SUFFIX, REQUESTS_CONFIG, RUN_BY_INTERCEPTOR, INTERCEPTORS, GET_REQUEST_CACHE, CLEAR_REQUESTS_CACHE, INCORRECT_PAYLOAD_ERROR */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "SUCCESS_SUFFIX", function() { return SUCCESS_SUFFIX; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ERROR_SUFFIX", function() { return ERROR_SUFFIX; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ABORT_SUFFIX", function() { return ABORT_SUFFIX; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "REQUESTS_CONFIG", function() { return REQUESTS_CONFIG; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "RUN_BY_INTERCEPTOR", function() { return RUN_BY_INTERCEPTOR; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "INTERCEPTORS", function() { return INTERCEPTORS; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "GET_REQUEST_CACHE", function() { return GET_REQUEST_CACHE; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "CLEAR_REQUESTS_CACHE", function() { return CLEAR_REQUESTS_CACHE; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "INCORRECT_PAYLOAD_ERROR", function() { return INCORRECT_PAYLOAD_ERROR; });
var SUCCESS_SUFFIX = '_SUCCESS';
var ERROR_SUFFIX = '_ERROR';
var ABORT_SUFFIX = '_ABORT';
var REQUESTS_CONFIG = 'REDUX_SAGA_REQUESTS_CONFIG';
var RUN_BY_INTERCEPTOR = 'RUN_BY_INTERCEPTOR';
var INTERCEPTORS = {
  ON_REQUEST: 'onRequest',
  ON_ERROR: 'onError',
  ON_SUCCESS: 'onSuccess',
  ON_ABORT: 'onAbort'
};
var GET_REQUEST_CACHE = 'GET_REQUEST_CACHE';
var CLEAR_REQUESTS_CACHE = 'CLEAR_REQUESTS_CACHE';
var INCORRECT_PAYLOAD_ERROR = "Incorrect payload for request action. Action must have form of { type: 'TYPE', request: {} }, { type: 'TYPE', request: [{}, {}] }, { type: 'TYPE', payload: { request: {} } } or { type: 'TYPE', payload: { request: [{}, {}] } }";

/***/ }),

/***/ "./src/index.js":
/*!**********************!*\
  !*** ./src/index.js ***!
  \**********************/
/*! exports provided: success, error, abort, getRequestCache, clearRequestsCache, createRequestsReducer, requestsReducer, requestsPromiseMiddleware, requestsCacheMiddleware, serverRequestsFilterMiddleware, createRequestInstance, getRequestInstance, sendRequest, watchRequests, countServerRequests */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _actions__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./actions */ "./src/actions.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "success", function() { return _actions__WEBPACK_IMPORTED_MODULE_0__["success"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "error", function() { return _actions__WEBPACK_IMPORTED_MODULE_0__["error"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "abort", function() { return _actions__WEBPACK_IMPORTED_MODULE_0__["abort"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "getRequestCache", function() { return _actions__WEBPACK_IMPORTED_MODULE_0__["getRequestCache"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "clearRequestsCache", function() { return _actions__WEBPACK_IMPORTED_MODULE_0__["clearRequestsCache"]; });

/* harmony import */ var _reducers__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./reducers */ "./src/reducers.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "createRequestsReducer", function() { return _reducers__WEBPACK_IMPORTED_MODULE_1__["createRequestsReducer"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "requestsReducer", function() { return _reducers__WEBPACK_IMPORTED_MODULE_1__["requestsReducer"]; });

/* harmony import */ var _middleware__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./middleware */ "./src/middleware.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "requestsPromiseMiddleware", function() { return _middleware__WEBPACK_IMPORTED_MODULE_2__["requestsPromiseMiddleware"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "requestsCacheMiddleware", function() { return _middleware__WEBPACK_IMPORTED_MODULE_2__["requestsCacheMiddleware"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "serverRequestsFilterMiddleware", function() { return _middleware__WEBPACK_IMPORTED_MODULE_2__["serverRequestsFilterMiddleware"]; });

/* harmony import */ var _sagas__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./sagas */ "./src/sagas.js");
/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "createRequestInstance", function() { return _sagas__WEBPACK_IMPORTED_MODULE_3__["createRequestInstance"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "getRequestInstance", function() { return _sagas__WEBPACK_IMPORTED_MODULE_3__["getRequestInstance"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "sendRequest", function() { return _sagas__WEBPACK_IMPORTED_MODULE_3__["sendRequest"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "watchRequests", function() { return _sagas__WEBPACK_IMPORTED_MODULE_3__["watchRequests"]; });

/* harmony reexport (safe) */ __webpack_require__.d(__webpack_exports__, "countServerRequests", function() { return _sagas__WEBPACK_IMPORTED_MODULE_3__["countServerRequests"]; });






/***/ }),

/***/ "./src/middleware.js":
/*!***************************!*\
  !*** ./src/middleware.js ***!
  \***************************/
/*! exports provided: requestsPromiseMiddleware, requestsCacheMiddleware, serverRequestsFilterMiddleware */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestsPromiseMiddleware", function() { return requestsPromiseMiddleware; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestsCacheMiddleware", function() { return requestsCacheMiddleware; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "serverRequestsFilterMiddleware", function() { return serverRequestsFilterMiddleware; });
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/extends */ "../../node_modules/@babel/runtime/helpers/extends.js");
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _constants__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./constants */ "./src/constants.js");
/* harmony import */ var _actions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./actions */ "./src/actions.js");




var shouldActionBePromisified = function shouldActionBePromisified(action, auto) {
  return auto && !(action.meta && action.meta.asPromise === false) || action.meta && action.meta.asPromise;
};

var requestsPromiseMiddleware = function requestsPromiseMiddleware(_temp) {
  var _ref = _temp === void 0 ? {} : _temp,
      _ref$auto = _ref.auto,
      auto = _ref$auto === void 0 ? false : _ref$auto;

  var requestMap = new Map();
  return function () {
    return function (next) {
      return function (action) {
        if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isRequestAction"])(action) && shouldActionBePromisified(action, auto)) {
          return new Promise(function (resolve, reject) {
            requestMap.set(action, function (response, error) {
              return error ? reject(response) : resolve(response);
            });
            next(action);
          });
        }

        if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isResponseAction"])(action)) {
          var requestAction = Object(_actions__WEBPACK_IMPORTED_MODULE_2__["getRequestActionFromResponse"])(action);

          if (shouldActionBePromisified(requestAction, auto)) {
            var requestActionPromise = requestMap.get(requestAction);
            requestActionPromise(action, action.type !== Object(_actions__WEBPACK_IMPORTED_MODULE_2__["success"])(requestAction.type));
            requestMap.delete(requestAction);
          }
        }

        return next(action);
      };
    };
  };
};

var isCacheValid = function isCacheValid(cache) {
  return cache.expiring === null || Date.now() <= cache.expiring;
};

var getNewCacheTimeout = function getNewCacheTimeout(cache) {
  return cache === true ? null : cache * 1000 + Date.now();
};

var getCacheKey = function getCacheKey(action) {
  return action.type + (action.meta.cacheKey || '');
};

var requestsCacheMiddleware = function requestsCacheMiddleware() {
  var cacheMap = new Map();
  return function () {
    return function (next) {
      return function (action) {
        if (action.type === _constants__WEBPACK_IMPORTED_MODULE_1__["GET_REQUEST_CACHE"]) {
          return cacheMap;
        }

        if (action.type === _constants__WEBPACK_IMPORTED_MODULE_1__["CLEAR_REQUESTS_CACHE"]) {
          if (action.actionTypes.length === 0) {
            cacheMap.clear();
          } else {
            action.actionTypes.forEach(function (actionType) {
              return cacheMap.delete(actionType);
            });
          }

          return null;
        }

        if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isRequestAction"])(action) && action.meta && action.meta.cache) {
          var cacheKey = getCacheKey(action);
          var cacheValue = cacheMap.get(cacheKey);

          if (cacheValue && isCacheValid(cacheValue)) {
            return next(_babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({}, action, {
              meta: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_0___default()({}, action.meta, {
                cacheResponse: cacheValue.response
              })
            }));
          } else if (cacheValue && !isCacheValid(cacheValue)) {
            cacheMap.delete(cacheKey);
          }
        } else if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isSuccessAction"])(action) && action.meta && action.meta.cache && !action.meta.cacheResponse) {
          var requestAction = Object(_actions__WEBPACK_IMPORTED_MODULE_2__["getRequestActionFromResponse"])(action);

          if (action.meta.cacheKey && action.meta.cacheSize) {
            var currentCacheKeys = Array.from(cacheMap.keys()).filter(function (k) {
              return k.startsWith(requestAction.type);
            });

            if (action.meta.cacheSize === currentCacheKeys.length) {
              cacheMap.delete(currentCacheKeys[0]);
            }
          }

          cacheMap.set(getCacheKey(requestAction), {
            response: Object(_actions__WEBPACK_IMPORTED_MODULE_2__["getActionPayload"])(action).response,
            expiring: getNewCacheTimeout(action.meta.cache)
          });
        }

        return next(action);
      };
    };
  };
};
var serverRequestsFilterMiddleware = function serverRequestsFilterMiddleware(_ref2) {
  var serverRequestActions = _ref2.serverRequestActions;
  var actionsToBeIgnored = serverRequestActions.slice();
  return function () {
    return function (next) {
      return function (action) {
        if (!Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isRequestAction"])(action)) {
          return next(action);
        }

        var actionToBeIgnoredIndex = actionsToBeIgnored.findIndex(function (a) {
          return a.type === action.type;
        });

        if (actionToBeIgnoredIndex === -1) {
          return next(action);
        }

        actionsToBeIgnored.splice(actionToBeIgnoredIndex, 1);
        return null;
      };
    };
  };
};

/***/ }),

/***/ "./src/reducers.js":
/*!*************************!*\
  !*** ./src/reducers.js ***!
  \*************************/
/*! exports provided: createRequestsReducer, requestsReducer */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createRequestsReducer", function() { return createRequestsReducer; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "requestsReducer", function() { return requestsReducer; });
/* harmony import */ var _babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/helpers/objectWithoutPropertiesLoose */ "../../node_modules/@babel/runtime/helpers/objectWithoutPropertiesLoose.js");
/* harmony import */ var _babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/extends */ "../../node_modules/@babel/runtime/helpers/extends.js");
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _actions__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./actions */ "./src/actions.js");



function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }

function _toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }

 // to support libraries like redux-act and redux-actions

var normalizeActionType = function normalizeActionType(actionType) {
  return typeof actionType === 'function' ? actionType.toString() : actionType;
};

var operationConfigHasRequestKey = function operationConfigHasRequestKey(config) {
  return typeof config !== 'boolean' && !!config.getRequestKey;
};

var getInitialRequestState = function getInitialRequestState(_ref) {
  var getDefaultData = _ref.getDefaultData,
      multiple = _ref.multiple,
      operations = _ref.operations;
  return {
    data: getDefaultData(multiple),
    pending: 0,
    error: null,
    operations: operations && Object.entries(operations).reduce(function (prev, _ref2) {
      var _extends2;

      var k = _ref2[0],
          v = _ref2[1];
      return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, prev, (_extends2 = {}, _extends2[k] = operationConfigHasRequestKey(v) ? {} : {
        error: null,
        pending: 0
      }, _extends2));
    }, {})
  };
};

var getInitialState = function getInitialState(state, reducer, config) {
  if (!reducer) {
    return getInitialRequestState(config);
  }

  return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, getInitialRequestState(config), reducer(undefined, {}));
};

var getDataUpdaterForSuccess = function getDataUpdaterForSuccess(reducerConfig, operationConfig) {
  if (operationConfig === true || typeof operationConfig !== 'boolean' && operationConfig.updateData === true) {
    return reducerConfig.updateData || reducerConfig.getData;
  } else if (typeof operationConfig === 'function') {
    return operationConfig;
  } else if (typeof operationConfig !== 'boolean' && typeof operationConfig.updateData !== 'boolean') {
    return operationConfig.updateData;
  }

  return null;
};

var defaultConfig = {
  multiple: false,
  getDefaultData: function getDefaultData(multiple) {
    return multiple ? [] : null;
  },
  getData: function getData(state, action) {
    return action.payload ? action.payload.data : action.data;
  },
  updateData: null,
  getError: function getError(state, action) {
    return action.payload ? action.payload : action.error;
  },
  onRequest: function onRequest(state) {
    return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, state, {
      pending: state.pending + 1,
      error: null
    });
  },
  onSuccess: function onSuccess(state, action, config) {
    return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, state, {
      data: config.getData(state, action, config),
      pending: state.pending - 1,
      error: null
    });
  },
  onError: function onError(state, action, config) {
    return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, state, {
      data: config.getDefaultData(config.multiple),
      pending: state.pending - 1,
      error: config.getError(state, action, config)
    });
  },
  onAbort: function onAbort(state) {
    return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, state, {
      pending: state.pending - 1
    });
  },
  resetOn: [],
  operations: null
};
var createRequestsReducer = function createRequestsReducer(globalConfig) {
  if (globalConfig === void 0) {
    globalConfig = {};
  }

  return function (localConfig, reducer) {
    if (reducer === void 0) {
      reducer = null;
    }

    var config = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, defaultConfig, globalConfig, localConfig);

    var onRequest = config.onRequest,
        onSuccess = config.onSuccess,
        onError = config.onError,
        onAbort = config.onAbort,
        resetOn = config.resetOn,
        getError = config.getError,
        operations = config.operations,
        actionType = config.actionType;
    var normalizedActionType = normalizeActionType(actionType);
    var shouldResetForAction = resetOn;

    if (typeof resetOn !== 'function') {
      var normalizedResetActions = resetOn.map(normalizeActionType);

      shouldResetForAction = function shouldResetForAction(action) {
        return normalizedResetActions.includes(action.type);
      };
    }

    return function (state, action) {
      var nextState = state === undefined ? getInitialState(state, reducer, config) : state;

      if (shouldResetForAction(action)) {
        nextState = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, getInitialState(nextState, reducer, config), {
          pending: nextState.pending
        });
      }

      if (operations && action.type in operations) {
        var _extends3, _extends4;

        var operationConfig = operations[action.type];
        return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState, {
          data: operationConfig.updateDataOptimistic ? operationConfig.updateDataOptimistic(nextState, action, config) : nextState.data,
          operations: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState.operations, (_extends4 = {}, _extends4[action.type] = operationConfigHasRequestKey(operationConfig) ? _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState.operations[action.type], (_extends3 = {}, _extends3[operationConfig.getRequestKey(action)] = {
            error: null,
            pending: nextState.operations[action.type][operationConfig.getRequestKey(action)] ? nextState.operations[action.type][operationConfig.getRequestKey(action)].pending + 1 : 1
          }, _extends3)) : {
            error: null,
            pending: nextState.operations[action.type].pending + 1
          }, _extends4))
        });
      }

      if (operations && Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isResponseAction"])(action) && Object(_actions__WEBPACK_IMPORTED_MODULE_2__["getRequestActionFromResponse"])(action).type in operations) {
        var _extends10;

        var requestAction = Object(_actions__WEBPACK_IMPORTED_MODULE_2__["getRequestActionFromResponse"])(action);
        var _operationConfig = operations[requestAction.type];

        var _nextState$operations = nextState.operations,
            currentOperation = _nextState$operations[requestAction.type],
            otherOperations = _babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0___default()(_nextState$operations, [requestAction.type].map(_toPropertyKey));

        if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isSuccessAction"])(action)) {
          var _extends6;

          var dataUpdater = getDataUpdaterForSuccess(config, _operationConfig);

          var _getUpdatedCurrentOperation = function _getUpdatedCurrentOperation() {
            if (!operationConfigHasRequestKey(_operationConfig)) {
              return {
                error: null,
                pending: currentOperation.pending - 1
              };
            }

            var currentRequestKey = _operationConfig.getRequestKey(requestAction);

            var operationForRequestKey = currentOperation[currentRequestKey],
                remainingOperations = _babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0___default()(currentOperation, [currentRequestKey].map(_toPropertyKey));

            if (operationForRequestKey.pending !== 1) {
              var _extends5;

              return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, remainingOperations, (_extends5 = {}, _extends5[currentRequestKey] = {
                error: null,
                pending: operationForRequestKey.pending - 1
              }, _extends5));
            }

            return remainingOperations;
          };

          return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState, {
            data: dataUpdater ? dataUpdater(nextState, action, config) : nextState.data,
            operations: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, otherOperations, (_extends6 = {}, _extends6[requestAction.type] = _getUpdatedCurrentOperation(), _extends6))
          });
        }

        if (Object(_actions__WEBPACK_IMPORTED_MODULE_2__["isErrorAction"])(action)) {
          var _extends7, _extends8;

          return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState, {
            data: _operationConfig.revertData ? _operationConfig.revertData(nextState, action, config) : nextState.data,
            operations: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, otherOperations, (_extends8 = {}, _extends8[requestAction.type] = operationConfigHasRequestKey(_operationConfig) ? _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, currentOperation, (_extends7 = {}, _extends7[_operationConfig.getRequestKey(requestAction)] = {
              error: getError(nextState, action, config),
              pending: currentOperation[_operationConfig.getRequestKey(requestAction)].pending - 1
            }, _extends7)) : {
              error: getError(nextState, action, config),
              pending: currentOperation.pending - 1
            }, _extends8))
          });
        } // abort case


        var getUpdatedCurrentOperation = function getUpdatedCurrentOperation() {
          if (!operationConfigHasRequestKey(_operationConfig)) {
            return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, currentOperation, {
              pending: currentOperation.pending - 1
            });
          }

          var currentRequestKey = _operationConfig.getRequestKey(requestAction);

          var operationForRequestKey = currentOperation[currentRequestKey],
              remainingOperations = _babel_runtime_helpers_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_0___default()(currentOperation, [currentRequestKey].map(_toPropertyKey));

          if (operationForRequestKey.pending !== 1) {
            var _extends9;

            return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, remainingOperations, (_extends9 = {}, _extends9[currentRequestKey] = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, operationForRequestKey, {
              pending: operationForRequestKey.pending - 1
            }), _extends9));
          }

          return remainingOperations;
        };

        return _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, nextState, {
          data: _operationConfig.revertData ? _operationConfig.revertData(nextState, action, config) : nextState.data,
          operations: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, otherOperations, (_extends10 = {}, _extends10[requestAction.type] = getUpdatedCurrentOperation(), _extends10))
        });
      }

      switch (action.type) {
        case normalizedActionType:
          return onRequest(nextState, action, config);

        case Object(_actions__WEBPACK_IMPORTED_MODULE_2__["success"])(normalizedActionType):
          return onSuccess(nextState, action, config);

        case Object(_actions__WEBPACK_IMPORTED_MODULE_2__["error"])(normalizedActionType):
          return onError(nextState, action, config);

        case Object(_actions__WEBPACK_IMPORTED_MODULE_2__["abort"])(normalizedActionType):
          return onAbort(nextState, action, config);

        default:
          return reducer ? reducer(nextState, action) : nextState;
      }
    };
  };
};
var requestsReducer = createRequestsReducer();

/***/ }),

/***/ "./src/sagas.js":
/*!**********************!*\
  !*** ./src/sagas.js ***!
  \**********************/
/*! exports provided: voidCallback, defaultConfig, createRequestInstance, getRequestsConfig, getRequestInstance, sendRequest, cancelSendRequestOnAction, watchRequests, countServerRequests */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "voidCallback", function() { return voidCallback; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "defaultConfig", function() { return defaultConfig; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createRequestInstance", function() { return createRequestInstance; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRequestsConfig", function() { return getRequestsConfig; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "getRequestInstance", function() { return getRequestInstance; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "sendRequest", function() { return sendRequest; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cancelSendRequestOnAction", function() { return cancelSendRequestOnAction; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "watchRequests", function() { return watchRequests; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "countServerRequests", function() { return countServerRequests; });
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ "../../node_modules/@babel/runtime/regenerator/index.js");
/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/extends */ "../../node_modules/@babel/runtime/helpers/extends.js");
/* harmony import */ var _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! redux-saga/effects */ "redux-saga/effects");
/* harmony import */ var redux_saga_effects__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var redux_saga__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! redux-saga */ "redux-saga");
/* harmony import */ var redux_saga__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(redux_saga__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _actions__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./actions */ "./src/actions.js");
/* harmony import */ var _constants__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./constants */ "./src/constants.js");



var _marked =
/*#__PURE__*/
_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(getRequestInstance),
    _marked2 =
/*#__PURE__*/
_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(sendRequest),
    _marked3 =
/*#__PURE__*/
_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(cancelSendRequestOnAction),
    _marked4 =
/*#__PURE__*/
_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(watchRequests),
    _marked5 =
/*#__PURE__*/
_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(countServerRequests);





/* eslint-disable */

var delay = __webpack_require__(/*! redux-saga */ "redux-saga").delay || __webpack_require__(/*! @redux-saga/delay-p */ "@redux-saga/delay-p").default;
/* eslint-enable */


var voidCallback = function voidCallback() {};
var defaultConfig = {
  driver: null,
  onRequest: null,
  onSuccess: null,
  onError: null,
  onAbort: null
};
function createRequestInstance(config) {
  var _setContext;

  return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["setContext"])((_setContext = {}, _setContext[_constants__WEBPACK_IMPORTED_MODULE_5__["REQUESTS_CONFIG"]] = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, defaultConfig, config), _setContext));
}
function getRequestsConfig() {
  return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["getContext"])(_constants__WEBPACK_IMPORTED_MODULE_5__["REQUESTS_CONFIG"]);
}
function getRequestInstance(driverType) {
  var config, driver;
  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function getRequestInstance$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          if (driverType === void 0) {
            driverType = null;
          }

          _context.next = 3;
          return getRequestsConfig();

        case 3:
          config = _context.sent;
          driver = driverType ? config.driver[driverType] : config.driver.default || config.driver;
          return _context.abrupt("return", driver.requestInstance);

        case 6:
        case "end":
          return _context.stop();
      }
    }
  }, _marked, this);
}

var getDriver = function getDriver(requestsConfig, action) {
  return action.meta && action.meta.driver ? requestsConfig.driver[action.meta.driver] : requestsConfig.driver.default || requestsConfig.driver;
};

function sendRequest(action, _temp) {
  var _ref, _ref$dispatchRequestA, dispatchRequestAction, _ref$silent, silent, _ref$runOnRequest, runOnRequest, _ref$runOnSuccess, runOnSuccess, _ref$runOnError, runOnError, _ref$runOnAbort, runOnAbort, runByInterceptor, requestsConfig, driver, actionPayload, _setContext2, abortSource, response, responseError, _setContext3, _ref2, onErrorResponse, onErrorError, errorPayload, _setContext4, successPayload, _setContext5;

  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function sendRequest$(_context2) {
    while (1) {
      switch (_context2.prev = _context2.next) {
        case 0:
          _ref = _temp === void 0 ? {} : _temp, _ref$dispatchRequestA = _ref.dispatchRequestAction, dispatchRequestAction = _ref$dispatchRequestA === void 0 ? false : _ref$dispatchRequestA, _ref$silent = _ref.silent, silent = _ref$silent === void 0 ? false : _ref$silent, _ref$runOnRequest = _ref.runOnRequest, runOnRequest = _ref$runOnRequest === void 0 ? null : _ref$runOnRequest, _ref$runOnSuccess = _ref.runOnSuccess, runOnSuccess = _ref$runOnSuccess === void 0 ? null : _ref$runOnSuccess, _ref$runOnError = _ref.runOnError, runOnError = _ref$runOnError === void 0 ? null : _ref$runOnError, _ref$runOnAbort = _ref.runOnAbort, runOnAbort = _ref$runOnAbort === void 0 ? null : _ref$runOnAbort;

          if (Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isRequestAction"])(action)) {
            _context2.next = 3;
            break;
          }

          throw new Error(_constants__WEBPACK_IMPORTED_MODULE_5__["INCORRECT_PAYLOAD_ERROR"]);

        case 3:
          _context2.next = 5;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["getContext"])(_constants__WEBPACK_IMPORTED_MODULE_5__["RUN_BY_INTERCEPTOR"]);

        case 5:
          runByInterceptor = _context2.sent;
          _context2.next = 8;
          return getRequestsConfig();

        case 8:
          requestsConfig = _context2.sent;

          if (!(dispatchRequestAction && !silent)) {
            _context2.next = 16;
            break;
          }

          action = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, action, {
            meta: _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, action.meta, {
              runByWatcher: false
            })
          });
          _context2.next = 13;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(action);

        case 13:
          action = _context2.sent;

          if (!(action === null)) {
            _context2.next = 16;
            break;
          }

          return _context2.abrupt("return", {
            serverSide: true
          });

        case 16:
          _context2.next = 18;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(getDriver, requestsConfig, action);

        case 18:
          driver = _context2.sent;
          actionPayload = Object(_actions__WEBPACK_IMPORTED_MODULE_4__["getActionPayload"])(action);

          if (!(requestsConfig.onRequest && (runOnRequest !== null ? runOnRequest : runByInterceptor !== _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_REQUEST))) {
            _context2.next = 26;
            break;
          }

          _context2.next = 23;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["setContext"])((_setContext2 = {}, _setContext2[_constants__WEBPACK_IMPORTED_MODULE_5__["RUN_BY_INTERCEPTOR"]] = _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_REQUEST, _setContext2));

        case 23:
          _context2.next = 25;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(requestsConfig.onRequest, actionPayload.request, action);

        case 25:
          actionPayload.request = _context2.sent;

        case 26:
          abortSource = driver.getAbortSource();
          _context2.prev = 27;
          _context2.prev = 28;

          if (!(action.meta && action.meta.cacheResponse)) {
            _context2.next = 33;
            break;
          }

          response = action.meta.cacheResponse;
          _context2.next = 42;
          break;

        case 33:
          if (Array.isArray(actionPayload.request)) {
            _context2.next = 39;
            break;
          }

          _context2.next = 36;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])([driver, 'sendRequest'], actionPayload.request, abortSource, action);

        case 36:
          response = _context2.sent;
          _context2.next = 42;
          break;

        case 39:
          _context2.next = 41;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["all"])(actionPayload.request.map(function (requestItem) {
            return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])([driver, 'sendRequest'], requestItem, abortSource, action);
          }));

        case 41:
          response = _context2.sent;

        case 42:
          _context2.next = 47;
          break;

        case 44:
          _context2.prev = 44;
          _context2.t0 = _context2["catch"](28);
          responseError = _context2.t0;

        case 47:
          if (!responseError) {
            _context2.next = 65;
            break;
          }

          if (!(requestsConfig.onError && (runOnError !== null ? runOnError : runByInterceptor !== _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_ERROR))) {
            _context2.next = 57;
            break;
          }

          _context2.next = 51;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["setContext"])((_setContext3 = {}, _setContext3[_constants__WEBPACK_IMPORTED_MODULE_5__["RUN_BY_INTERCEPTOR"]] = _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_ERROR, _setContext3));

        case 51:
          _context2.next = 53;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(requestsConfig.onError, responseError, action);

        case 53:
          _ref2 = _context2.sent;
          onErrorResponse = _ref2.response;
          onErrorError = _ref2.error;

          if (onErrorResponse) {
            response = onErrorResponse;
          } else {
            responseError = onErrorError;
          }

        case 57:
          if (response) {
            _context2.next = 65;
            break;
          }

          _context2.next = 60;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(driver.getErrorPayload, responseError);

        case 60:
          errorPayload = _context2.sent;

          if (silent) {
            _context2.next = 64;
            break;
          }

          _context2.next = 64;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(Object(_actions__WEBPACK_IMPORTED_MODULE_4__["createErrorAction"])(action, errorPayload));

        case 64:
          return _context2.abrupt("return", {
            error: responseError
          });

        case 65:
          if (!(requestsConfig.onSuccess && (runOnSuccess !== null ? runOnSuccess : runByInterceptor !== _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_SUCCESS && runByInterceptor !== _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_ERROR))) {
            _context2.next = 71;
            break;
          }

          _context2.next = 68;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["setContext"])((_setContext4 = {}, _setContext4[_constants__WEBPACK_IMPORTED_MODULE_5__["RUN_BY_INTERCEPTOR"]] = _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_SUCCESS, _setContext4));

        case 68:
          _context2.next = 70;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(requestsConfig.onSuccess, response, action);

        case 70:
          response = _context2.sent;

        case 71:
          _context2.next = 73;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(driver.getSuccessPayload, response, actionPayload.request);

        case 73:
          successPayload = _context2.sent;

          if (silent) {
            _context2.next = 77;
            break;
          }

          _context2.next = 77;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(Object(_actions__WEBPACK_IMPORTED_MODULE_4__["createSuccessAction"])(action, successPayload, response));

        case 77:
          return _context2.abrupt("return", {
            response: response
          });

        case 78:
          _context2.prev = 78;
          _context2.next = 81;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["cancelled"])();

        case 81:
          if (!_context2.sent) {
            _context2.next = 92;
            break;
          }

          _context2.next = 84;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])([driver, 'abortRequest'], abortSource);

        case 84:
          if (!(requestsConfig.onAbort && (runOnAbort !== null ? runOnAbort : runByInterceptor !== _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_ABORT))) {
            _context2.next = 89;
            break;
          }

          _context2.next = 87;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["setContext"])((_setContext5 = {}, _setContext5[_constants__WEBPACK_IMPORTED_MODULE_5__["RUN_BY_INTERCEPTOR"]] = _constants__WEBPACK_IMPORTED_MODULE_5__["INTERCEPTORS"].ON_ABORT, _setContext5));

        case 87:
          _context2.next = 89;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(requestsConfig.onAbort, action);

        case 89:
          if (silent) {
            _context2.next = 92;
            break;
          }

          _context2.next = 92;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(Object(_actions__WEBPACK_IMPORTED_MODULE_4__["createAbortAction"])(action));

        case 92:
          return _context2.finish(78);

        case 93:
        case "end":
          return _context2.stop();
      }
    }
  }, _marked2, this, [[27,, 78, 93], [28, 44]]);
}

var isRequestReadOnly = function isRequestReadOnly(request) {
  return !request.query && (!request.method || request.method.toLowerCase() === 'get') || request.query && !request.query.trim().startsWith('mutation');
};

var watchRequestsDefaultConfig = {
  takeLatest: function takeLatest(action) {
    var _getActionPayload = Object(_actions__WEBPACK_IMPORTED_MODULE_4__["getActionPayload"])(action),
        request = _getActionPayload.request;

    return Array.isArray(request) ? request.every(isRequestReadOnly) : isRequestReadOnly(request);
  },
  abortOn: null,
  getLastActionKey: function getLastActionKey(action) {
    return action.type;
  }
};
function cancelSendRequestOnAction(abortOn, task) {
  var _ref3, abortingAction;

  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function cancelSendRequestOnAction$(_context3) {
    while (1) {
      switch (_context3.prev = _context3.next) {
        case 0:
          _context3.next = 2;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["race"])({
            abortingAction: Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["take"])(abortOn),
            taskFinished: Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["join"])(task),
            timeout: Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["call"])(delay, 10000) // taskFinished doesnt work for aborted tasks

          });

        case 2:
          _ref3 = _context3.sent;
          abortingAction = _ref3.abortingAction;

          if (!abortingAction) {
            _context3.next = 7;
            break;
          }

          _context3.next = 7;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["cancel"])(task);

        case 7:
        case "end":
          return _context3.stop();
      }
    }
  }, _marked3, this);
}

var isWatchable = function isWatchable(a) {
  return Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isRequestAction"])(a) && (!a.meta || a.meta.runByWatcher !== false);
};

function watchRequests(commonConfig) {
  var lastTasks, config, action, lastActionKey, takeLatest, activeTask, newTask, abortOn;
  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function watchRequests$(_context4) {
    while (1) {
      switch (_context4.prev = _context4.next) {
        case 0:
          if (commonConfig === void 0) {
            commonConfig = {};
          }

          lastTasks = {};
          config = _babel_runtime_helpers_extends__WEBPACK_IMPORTED_MODULE_1___default()({}, watchRequestsDefaultConfig, commonConfig);

        case 3:
          if (false) {}

          _context4.next = 6;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["take"])(isWatchable);

        case 6:
          action = _context4.sent;
          lastActionKey = config.getLastActionKey(action);
          takeLatest = action.meta && action.meta.takeLatest !== undefined ? action.meta.takeLatest : typeof config.takeLatest === 'function' ? config.takeLatest(action) : config.takeLatest;

          if (!takeLatest) {
            _context4.next = 14;
            break;
          }

          activeTask = lastTasks[lastActionKey];

          if (!activeTask) {
            _context4.next = 14;
            break;
          }

          _context4.next = 14;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["cancel"])(activeTask);

        case 14:
          _context4.next = 16;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["fork"])(sendRequest, action);

        case 16:
          newTask = _context4.sent;

          if (takeLatest) {
            lastTasks[lastActionKey] = newTask;
          }

          abortOn = action.meta && action.meta.abortOn ? action.meta.abortOn : config.abortOn;

          if (!abortOn) {
            _context4.next = 22;
            break;
          }

          _context4.next = 22;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["fork"])(cancelSendRequestOnAction, abortOn, newTask);

        case 22:
          _context4.next = 3;
          break;

        case 24:
        case "end":
          return _context4.stop();
      }
    }
  }, _marked4, this);
}
function countServerRequests(_ref4) {
  var serverRequestActions, _ref4$finishOnFirstEr, finishOnFirstError, index, action;

  return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function countServerRequests$(_context5) {
    while (1) {
      switch (_context5.prev = _context5.next) {
        case 0:
          serverRequestActions = _ref4.serverRequestActions, _ref4$finishOnFirstEr = _ref4.finishOnFirstError, finishOnFirstError = _ref4$finishOnFirstEr === void 0 ? true : _ref4$finishOnFirstEr;
          index = 0;
          serverRequestActions.requestActionsToIgnore = [];
          serverRequestActions.successActions = [];
          serverRequestActions.dependentSuccessActions = [];
          serverRequestActions.errorActions = [];

        case 6:
          if (false) {}

          _context5.next = 9;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["take"])(function (a) {
            return Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isRequestAction"])(a) || Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isResponseAction"])(a);
          });

        case 9:
          action = _context5.sent;

          if (!Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isRequestAction"])(action)) {
            _context5.next = 13;
            break;
          }

          index += action.meta && action.meta.dependentRequestsNumber !== undefined ? action.meta.dependentRequestsNumber + 1 : 1;
          return _context5.abrupt("continue", 6);

        case 13:
          if (Object(_actions__WEBPACK_IMPORTED_MODULE_4__["isSuccessAction"])(action)) {
            _context5.next = 21;
            break;
          }

          serverRequestActions.errorActions.push(action);

          if (!finishOnFirstError) {
            _context5.next = 19;
            break;
          }

          _context5.next = 18;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(redux_saga__WEBPACK_IMPORTED_MODULE_3__["END"]);

        case 18:
          return _context5.abrupt("return");

        case 19:
          _context5.next = 22;
          break;

        case 21:
          if (action.meta.isDependentRequest) {
            serverRequestActions.dependentSuccessActions.push(action);
          } else {
            serverRequestActions.successActions.push(action);
          }

        case 22:
          index -= action.meta.isDependentRequest ? 2 : 1;

          if (!(index === 0)) {
            _context5.next = 28;
            break;
          }

          serverRequestActions.requestActionsToIgnore = serverRequestActions.successActions.map(_actions__WEBPACK_IMPORTED_MODULE_4__["getRequestActionFromResponse"]).map(function (a) {
            return {
              type: a.type
            };
          });
          _context5.next = 27;
          return Object(redux_saga_effects__WEBPACK_IMPORTED_MODULE_2__["put"])(redux_saga__WEBPACK_IMPORTED_MODULE_3__["END"]);

        case 27:
          return _context5.abrupt("return");

        case 28:
          _context5.next = 6;
          break;

        case 30:
        case "end":
          return _context5.stop();
      }
    }
  }, _marked5, this);
}

/***/ }),

/***/ "@redux-saga/delay-p":
/*!*****************************************************************************************************************!*\
  !*** external {"commonjs":"@redux-saga/delay-p","commonjs2":"@redux-saga/delay-p","amd":"@redux-saga/delay-p"} ***!
  \*****************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE__redux_saga_delay_p__;

/***/ }),

/***/ "redux-saga":
/*!*********************************************************************************************************!*\
  !*** external {"commonjs":"redux-saga","commonjs2":"redux-saga","amd":"redux-saga","root":"ReduxSaga"} ***!
  \*********************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_redux_saga__;

/***/ }),

/***/ "redux-saga/effects":
/*!*****************************************************************************************************************************************!*\
  !*** external {"commonjs":"redux-saga/effects","commonjs2":"redux-saga/effects","amd":"redux-saga/effects","root":"ReduxSaga.effects"} ***!
  \*****************************************************************************************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE_redux_saga_effects__;

/***/ })

/******/ });
});
//# sourceMappingURL=redux-saga-requests.js.map